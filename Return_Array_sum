static void sum(int[] arr){
        //edge cases
        int length=arr.length;
        int sum=0;

        if(length==0){
            return;
        }


        for(int i=0;i<length;i++){
            sum=sum+arr[i];
        }
        System.out.println(sum);
    }
